export const pokemonsList = (state) => state.pokemonsList;

export const getOffset = (state) => state.offset;

export const isLoading = (state) => state.loading;

export const getFavoritesPokemons = (state) => state.favoritesPokemons;

export const getPokemonsInfo = (state) => state.pokemonsInfo;
